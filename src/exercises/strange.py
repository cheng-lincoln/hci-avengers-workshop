from src.lib import next_alphabet

def crack(previous_guess, time_taken):
    """
    If your previous guess was 'abcd', then:
    previous_guess[0] == 'a'
    previous_guess[1] == 'b'
    previous_guess[2] == 'c'
    previous_guess[3] == 'd'

    time_taken is the time it took for the computer
    to reply whether your previous guess was correct or not
    """
    guess = [c for c in previous_guess]
    force_stop = False

    #=== Start writing your code from here onwards! ===

    #=== Stop writing your code here! ===

    return (guess, force_stop)
